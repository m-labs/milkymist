CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(makeraw)

SET(CMAKE_MODULE_PATH ${PROJECT_SOURCE_DIR})

FIND_PACKAGE(GD REQUIRED)

ADD_EXECUTABLE(makeraw makeraw)

TARGET_LINK_LIBRARIES(makeraw ${GD_LIBRARY})
